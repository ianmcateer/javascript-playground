// Invokes a provided function once for each item in the array.
// Note .forEach is similar to .map except .map returns a new array,
// .forEach has no return value. Generally, you can use forEach as a replacement for a for loop.

const friends = ['Jake', 'Mikenzi', 'Jacob']

friends.forEach(friend => console.log( 'hello'))

