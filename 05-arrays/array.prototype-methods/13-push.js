// Adds an item to the end of the array and returns the array’s new length.
// Favor using .concat instead of .push as mutations are 👺.

const friends = ['Jake', 'Mikenzi']

friends.push('Jordyn') // 3

console.log(friends) ['Jake', 'Mikenzi', 'Jordyn']
