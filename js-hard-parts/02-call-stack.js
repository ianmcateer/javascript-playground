const num = 3
function multiplyBy2(inputNumber) {
  const result = inputNumber * 2
  return result
}
const name = 'Will'

const output = multipleBy2(4)
const newOutput = multipleBy2(10)
// () running a function

// call stack keeps track of execution context
// looks like this...
// whatever is top of call stack is wherere thread is running
// last in first out


// local execution context
// global execution context


// call stack keeps track of execution context
// local memory- also called variable enviornment