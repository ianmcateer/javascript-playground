// function as a first class citizen
// pure functions no side effects- one consequence
// when run a function its only consequence is determined by the return value nnot mutating anything 
// higher order function- takes a function as a parameter or/and returns a function as its return

function squareNum(num){
  return num * num
}
squareNum(10)